{
  "name": "style-me-extension",
  "version": "1.0.0",
  "description": "Virtually try on clothes using your own OpenRouter API key",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build-simple": "mkdir -p dist && cp -r public/* dist/ && cp -r src/* dist/"
  },
  "dependencies": {
    "@google/genai": "^1.16.0",
    "@huggingface/inference": "^4.7.1",
    "mime": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "vite": "^5.0.0",
    "vite-plugin-chrome-extension": "^0.0.7"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
